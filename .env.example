# Django settings
DJANGO_SECRET_KEY=replace-with-a-long-random-string
DJANGO_DEBUG=True
DJANGO_ENV=development
PHONE_API_KEY=replace-with-phone-api-key

# Google OAuth (owner login)
GOOGLE_OAUTH_CLIENT_ID=replace-with-google-client-id
GOOGLE_OAUTH_CLIENT_SECRET=replace-with-google-client-secret
# Optional: override redirect URI used in OAuth flow
# GOOGLE_OAUTH_REDIRECT_URI=https://your-domain.example/login/google/callback/

# Google OAuth (mobile app login)
# Comma-separated client IDs for Android/iOS/Web used by the Expo app.
# Example: GOOGLE_OAUTH_MOBILE_CLIENT_IDS=android-client-id.apps.googleusercontent.com,ios-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_MOBILE_CLIENT_IDS=

# Email (console backend by default; switch to SMTP in real env)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=1
EMAIL_HOST_USER=utm247700c@gmail.com
EMAIL_HOST_PASSWORD=udazagzwrpayqwpv 
DEFAULT_FROM_EMAIL=utm247700c@gmail.com
# Database (used only if you switch settings.py to read env values)
DB_ENGINE=django.db.backends.mysql
DB_NAME=ciquest_db
DB_USER=app_user
DB_PASSWORD=strong-password
DB_HOST=localhost
DB_PORT=3306

# Frontend/clients (optional; code currently uses relative paths)
# API_BASE_URL=http://13.222.11.15:8000
