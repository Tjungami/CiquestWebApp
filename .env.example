# Django settings
DJANGO_SECRET_KEY=replace-with-a-long-random-string
DJANGO_DEBUG=True
DJANGO_ENV=development
ALLOWED_HOSTS=localhost,127.0.0.1,13.222.11.15,ciquestwebapp.onrender.com

# Email (console backend by default; switch to SMTP in real env)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USE_TLS=1
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=no-reply@ciquest.local

# Database (used only if you switch settings.py to read env values)
DB_ENGINE=django.db.backends.mysql
DB_NAME=ciquest_db
DB_USER=app_user
DB_PASSWORD=strong-password
DB_HOST=localhost
DB_PORT=3306

# Frontend/clients (optional; code currently uses relative paths)
# API_BASE_URL=http://13.222.11.15:8000
